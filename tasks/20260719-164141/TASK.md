# Create a vim command keybind to open the issue found under cursor into a new window split

- STATUS: CLOSED
- PRIORITY: 100
- TAGS: neovim,feature


say you have in your code (e.g. C code):

```c
// TASK(20260703-171014): Change query evaluation method from eval as you to building an ast tree and go from there
/*
 * <code>
 */
```

Having your cursor (either) on the HUID (or maybe on just the line?), it will open a new split (like my Run <command> command does)

Related task:
task(20260719-163806): When creating a new task, copy it to the user's clipboard

