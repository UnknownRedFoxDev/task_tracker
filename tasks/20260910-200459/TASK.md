# Rename eval_node function to retrieve_tasks_from_query such that it can be used for other functions aswell

- STATUS: CLOSED
- PRIORITY: 100
- TAGS: internal-rework

In a Tsoding stream, he extended his tool's feature to not only take task huid but also to use his query language to do a mass-update tasks
and I think it's a based idea because it's what I wanted to do with the overwrite function for a while

the overwrite function is a bit annoying in the sense that you have the provide the task upfront and then do the modifications you want with it.

But what if I want to remove and add on a mass-tasks scale such as mass renaming a tag

