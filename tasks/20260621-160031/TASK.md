# Add a 'ls-rev' or 'ls-inv' to have priority ordered list reversed/inverted

- STATUS: OPEN
- PRIORITY: 10
- TAGS: feature,cmdline-options

# Concept
Either:

```bash
./tatr ls-rev [filters]
```

or

```bash
./tatr ls-inv [filters]
```

# What it does

Whatever I feel is the best when I'll get to it.
This will invert the order by which the list of task is printed.
