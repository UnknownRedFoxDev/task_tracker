# Add a 'open <task-id>' option to open a task using the user's nvim of choice

- STATUS: OPEN
- PRIORITY: 50
- TAGS: feature,cmdline-options

The title says it all.

Upon using `./tatr open <task-id>` it will open the task's markdown file using the $EDITOR environment variable.
Defaults to vim.

