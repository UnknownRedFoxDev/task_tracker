# Create a cmdline-option for the version of the tooling

- STATUS: OPEN
- PRIORITY: 100
- TAGS: cmdline-options,feature

use the `git rev-parse HEAD`

print whatever format you want when making it:

suggestion:
```
$ tatr version
tatr - Task Tracker
git version: <git-hash>
```
